{
  "name": "mern-stock-application",
  "version": "1.0.0",
  "description": "Full-stack MERN application with user authentication and stock data viewing",
  "main": "server/index.js",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm start",
    "install": "npm install && cd server && npm install && cd ../client && npm install",
    "start": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "keywords": ["MERN", "stock", "authentication"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
